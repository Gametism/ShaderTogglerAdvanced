# ShaderToggler (patched)

- Texture toggling: persisted to `ShaderToggler.ini`.
- Safe with ReShade 5.1+ SDK (no `push_descriptors` registration).
- Signatures use global `::CDataFile` and `ShaderToggler::ToggleGroup`.
